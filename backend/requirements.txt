Flask==2.3.3
Flask-CORS==4.0.0
requests==2.31.0
numpy==1.24.3
python-dotenv==1.0.0

# Optional but recommended:
# faiss-cpu==1.7.4  # For better vector search performance (requires compilation)
# sentence-transformers==2.2.2  # For better embeddings instead of simple TF-IDF